AOS.init({
    duration: 1200, // Durée de l'animation en millisecondes
    easing: 'ease-in-out', // Fonction de transition
    offset: 400, // Décalage de déclenchement de l'animation
    once: true, // Activer l'animation uniquement une fois (false pour animer à chaque défilement)
});
